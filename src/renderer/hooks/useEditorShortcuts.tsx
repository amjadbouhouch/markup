import { Editor } from '@tiptap/react';
import React, { useEffect } from 'react';

interface UseEditorShortcutsProps {
  editor: Editor;
}
const useEditorShortcuts = ({ editor }: UseEditorShortcutsProps) => {};

export default useEditorShortcuts;
